box: ruby
build:
  steps:
    - bundle-install
    - script:
        name: install build-deps
        code: sudo apt-get update && sudo apt-get install -y llvm
    - script:
        name: compile
        code: bundle exec rake compile
    - script:
        name: rspec
        code: bundle exec rake spec
