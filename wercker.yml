box: k0kubun/llvm38-ruby24
build:
  steps:
    - bundle-install
    - script:
        name: compile
        code: bundle exec rake compile
    - script:
        name: rspec
        code: bundle exec rake spec
