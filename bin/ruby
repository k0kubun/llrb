#!/usr/bin/env ruby

# Just for comparison with bin/llrb
require 'bundler/setup'
require 'llrb'

script = File.read(ARGV.first)
ruby = Class.new
ruby.instance_eval("def self.script; #{script}; end")

ruby.script

LLRB::JIT.precompile(ruby, :script)
