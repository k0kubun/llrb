#!/usr/bin/env ruby

require 'bundler/setup'
require 'pry'

script = File.read(ARGV.first || "#{__dir__}/scripts/bm_app_fib.rb")
ruby = Class.new
ruby.instance_eval("def self.script; #{script}; end")
result = RubyVM::InstructionSequence.of(ruby.method(:script)).to_a
Pry::ColorPrinter.pp(result)
