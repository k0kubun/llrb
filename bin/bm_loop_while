#!/usr/bin/env ruby

require 'bundler/setup'
require 'benchmark/ips'
require 'optparse'
require 'llrb'

llrb = Class.new
def llrb.loop_while
  i = 0
  while i< 6_000_000
    i += 1
  end
  i
end
LLRB::JIT.compile(llrb, :loop_while)

llrb.loop_while
